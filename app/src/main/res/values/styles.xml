<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/background_action__default</item>
        <item name="colorPrimaryDark">@color/background_action__emphasis</item>
        <item name="colorAccent">@color/background_action__default</item>
    </style>

</resources>
